-- ════════════════════════════════════════════════════════════════════
--  0028 — 속성(종족)별 순위표 삭제
--
--  신발 스탯에서 속성을 뺐으므로 "속성별 누적 거리" 순위도 없앤다(결정 2026-09-25).
--  앱(5단계)이 이 함수를 더 부르지 않는다. 0005 · 0006 이 만든 두 형태를 모두 지운다 —
--  setup.sql 은 처음부터 다시 올리므로 만들었다 지우는 순서가 매번 반복돼도 안전하다.
--  walk_sessions.faction 열은 지난 기록이라 그대로 둔다.
-- ════════════════════════════════════════════════════════════════════

drop function if exists public.faction_leaderboard();
drop function if exists public.faction_leaderboard(text);
