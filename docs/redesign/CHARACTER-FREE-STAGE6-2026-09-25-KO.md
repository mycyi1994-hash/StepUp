# 캐릭터 없는 리디자인 6단계 — 검수 수정 반영

## 이번 수정

5단계 Claude 검수의 필수 4묶음을 반영했다. 새 기능이나 새 이미지 생성 없이 공통 메뉴와 화면 배치를 수정했다.

| 묶음 | 반영 내용 | 유지한 동작 |
|---|---|---|
| 하단 메뉴 | 실제 표시 글자의 폭을 측정하고 긴 이름에 여유 폭을 배분. 뽑기를 화면 중앙에 두고 좌우 두 메뉴씩 각 영역 안에서 배분. 일반 글자가 들어가면 기존 균등 폭 사용. 글자 크기 강제 축소 없음 | 기존 4개 탭 + 뽑기 버튼, 선택 표시, 라우트, 시스템 글자 배율, 최소 터치 영역 |
| 작은 화면 러닝 | 러닝 중 2열은 내용 폭 300dp, 완료 카드 3열은 320dp부터 허용. 높이 800dp 미만에서는 지도 256dp로 버튼과 간격 확보. 큰 글자는 세로 배치 유지 | 실제 지도·출처, 통계 값, 일시정지·종료·완료 동작 |
| 프로필 | 아바타/이름/편집/설정을 가로 배치하고 최근 기록과 중복된 목록 행 제거. 기록 접근 태그를 기존 ‘전체 보기’로 이전 | 실제 프로필 이미지, 편집/설정/전체 기록/챌린지/지갑 경로 |
| 커뮤니티 | 함께 달리기 화면의 로그인 안내를 문구+로그인 버튼의 간결한 카드로 표시 | 기존 로그인 재진입 처리. 오류·로딩·준비 상태는 기존 표현 유지 |

선택적 배너 여백 조정은 이번 필수 수정에 포함하지 않았다. 4단계에서 해결한 문구/화살표 겹침을 다시 만들지 않는다.

## 검증

최초 소스 `9ad6209`는 빌드와 Android 14 전체 검사에 통과했다. Android 15 동작 검사 5개도 통과했으나, 작은 러닝 화면의 지도 아래 경계가 고정 버튼 위쪽을 넘는 배치 검사에 실패했다. `973a07c`에서 지도 높이를 보완했다. 이어 Android 14 캡처에서 메뉴 폭 배분이 가운데 뽑기 위치를 옮긴 것을 확인해 좌우 균형을 보완했다. 최종 소스는 `29bb16b4411a490cc78a2accc53790b6ca970ac2`다.

- 최초 실행: [빌드 36101459476](https://github.com/mycyi1994-hash/StepUp/actions/runs/36101459476), [기기 36101459423](https://github.com/mycyi1994-hash/StepUp/actions/runs/36101459423)
- 중간 빌드 36102117640 및 기기 실행 36102117632는 새 소스로 대체되어 취소됐으며 통과로 세지 않는다.
- 최종 빌드: [36102357282](https://github.com/mycyi1994-hash/StepUp/actions/runs/36102357282)
- 최종 Android 14·15: [36102357154](https://github.com/mycyi1994-hash/StepUp/actions/runs/36102357154)
- 최종 실행 결과: 단위 검사·일반 빌드·서명 확인·출시 설정 빌드 통과. Android 14·15 각각 동작 검사 5개 + 56장 캡처 검사 1개 통과. 양쪽 `ref-layout-checks.txt`도 PASS.

기존 14개 장면 × 4가지 화면/글자 설정 캡처에 다음 검사를 추가했다: 홈의 하단 메뉴 이름 한 줄 노출과 뽑기 중앙 정렬, 일반 글자 러닝 지도가 고정 버튼 위에 온전히 위치하는지, 프로필 전체 기록 버튼 노출, 커뮤니티 모임 제목이 고정 버튼 위에 있는지. 원본 캡처에서 직접 보이는 상태도 함께 확인한다.

정적 디자인 계약/문자열/자산 검사 통과. 데이터 저장 이름, 서버 처리, 소유/지급 상태 및 뽑기 배포 조건은 변경하지 않았다.

## 실제 캡처와 Claude 재확인

Android 14·15 각각 56장, 총 112장 캡처 및 결과 XML을 보존했다. `before-after.jpg`는 이전 검증 소스 `6e327f6`와 최종 소스 `29bb16b`의 실제 앱 캡처를 비교한 것이다. 원본 이미지에 새 UI를 합성한 시안이 아니다.

Claude Code에 읽기 전용으로 최종 Android 15 원본 7장만 전달해 같은 4묶음을 재확인했다. **4묶음 모두 해결됨, 해당 범위에 남은 차단 요소 없음**으로 판정했다. [Claude 재확인 원문](CLAUDE-VISUAL-RECHECK-2026-09-25.md)을 보존했다. 새로운 전체 앱 검수나 출시 승인은 요청하거나 주장하지 않았다.

자료:
- `before-after.jpg`: 메뉴/러닝/프로필/커뮤니티 4묶음 수정 전후.
- `gallery.html`: 최종 Android 15 원본 56장.
- `api34/`, `api35/`: 최종 원본과 실행 결과.
- `device-evidence.json`, `run-evidence.json`: 테스트 결과, 소스 버전, 캡처 해시.
- `claude-check/`: 재확인 요청문, 실제 읽은 7장, 결과 원문.

참고: 이번 최종 러닝 캡처에는 지도 타일이 표시되기 전 격자 배경이 보인다. 경로·지도 영역·출처·조작부의 배치를 확인한 자료이며, 실제 환경의 지도 타일 로딩 및 야외 GPS 검증을 대신하지 않는다. 큰 글자에서 스크롤 아래의 내용이 일부만 보이는 것과 글자 자체가 잘리는 것은 구분했다.

## 범위

이번 검증은 검수에서 지적한 주요 화면과 공통 메뉴의 수정 확인이다. 전체 상세/작성/설정 화면, 모든 언어/테마, 야외 GPS, 실제 인증/지갑 거래의 출시 승인을 뜻하지 않는다. 기본 뽑기 버튼 비활성화와 게스트+검사 게시물 조합은 기존 검사 환경 조건이다.

필수 4묶음은 반영·검증 완료다. 선택적 배너 여백 마감과 위에 열거한 별도 검증 범위는 남아 있다. 공개 APK 업로드, main 병합, 서비스 배포는 하지 않았다. 문서 커밋 이후에도 검증 소스는 `29bb16b`다.

결과 폴더: `C:/Users/gana0/OneDrive/문서/New project 3/output/stepup-character-free-stage6-2026-09-25/`.
